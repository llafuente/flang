fn reverseArray(i32 count) : void {
  var i32 i;
  printf("count = %d\n", count);
  for i = 0; i < count >> 1; i = i + 1 {
    var i32 lastIdx;
    lastIdx = count - i - 1;

    printf("i = %zu lastIdx = %zu\n", i, lastIdx);

  }
  return 0;
}

var i32 c;
c = 10;
reverseArray(10);
