/* string must be first atm */
#load "./../core/string.fl"

#load "./../core/ffi-c.fl"

fn alloc(u64 amount_of_bytes) : ptr<void> {
  return malloc(amount_of_bytes);
}

/*
ffi fn printf(ptr<i8> format, ...) : i32;
fn log(ptr<string> str) {
  printf("%s\n", str[0]->value);
}
*/
